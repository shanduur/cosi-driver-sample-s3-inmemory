apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sample-s3-inmemo.fullname" . }}-test-connection"
  labels:
    {{- include "sample-s3-inmemo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sample-s3-inmemo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
